# Expo HAS CHANGED

This project is on **Expo SDK 54** (`"expo": "~54.0.0"` in package.json).

Before writing code that touches an Expo SDK API, read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ — not the `latest` docs, which document a newer SDK and will
describe APIs this build does not have.

Keep the version above in step with package.json whenever the SDK is upgraded.
